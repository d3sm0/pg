program: main.py
method: bayes
metric:
  name: "train/return"
  goal: maximize
parameters:
  agent:
    values: [ "ppo"]
  penalty:
    distribution: uniform
    max: 0.4
    min: 0.1
  eta:
    distribution: uniform
    max: 0.5
    min: 0.01


