program: main.py
method: bayes
metric:
  name: "train/return"
  goal: maximize
parameters:
  agent:
    values: [ "pg_clip" ]
  eta:
    distribution: uniform
    max: 1.0
    min: 0.4
  penalty:
    distribution: uniform
    max: 1.0
    min: 0.1


