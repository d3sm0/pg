program: main.py
method: bayes
metric:
  name: "train/return"
  goal: maximize
parameters:
  agent:
    values: [ "pg_clip"]
  penalty:
    distribution: uniform
    max: 0.4
    min: 0.1
  eta:
    distribution: uniform
    max: 0.1
    min: 0.01


