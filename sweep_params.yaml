program: main.py
method: bayes
metric:
  name: return
  goal: maximize
parameters:
  learning_rate:
    distribution: uniform
    min: 0.01
    max: 1.0
  eta:
    distribution: uniform
    min: 0.0001
    max: 0.1
  opt_epochs:
    distribution: q_uniform
    min: 10
    max: 20
